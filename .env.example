# mock: use local demo data; rest: use the backend endpoints documented in README.md
VITE_API_MODE=mock
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT=15000
VITE_API_CREDENTIALS=same-origin

# Optional dev proxy. The backend must expose the /api prefix.
# VITE_API_PROXY_TARGET=http://localhost:8080
